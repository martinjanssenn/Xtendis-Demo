import pytest
# from src.app import app 

def test_page_id():
    assert True
    # for i in range(0, 10):
    #     response = app.test_client().get(f'/page/{i}')
    #     assert response.status_code == 200
    #     assert response.data == f'Page {i}'.encode()